#include <stdio.h>
/**
 * main -Programe that prints out programe in lowercase
 *
 * Return : 0 (Success)
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; l++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
